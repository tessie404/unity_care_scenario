/// <summary>Three interruptible night-shift tasks (labels match experiment spec).</summary>
public enum NightShiftTaskId
{
    C, // bed exit
    B, // wet floor / spilled water
    A  // incontinence
}